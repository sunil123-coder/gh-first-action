name: Third Workflow
on:
  workflow_dispatch:
jobs:
  first-job:
    runs-on: ubunut-latest
    steps:
      - name: Print greeting
        run: echo "Hello world"
      - name: Print goodbye
        run: echo "Done -Goodbye" 
